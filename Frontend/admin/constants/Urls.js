const AUTH_IP='192.168.100.59'
const AUTH_PORT='3000'
const USER_PORT='3001'
const DRIVER_PORT='3002'
const NEAREST_PORT='3003'
const ADMIN_PORT='3004'

const Urls = {
  auth: `${AUTH_IP}:${AUTH_PORT}`,
  user: `${AUTH_IP}:${USER_PORT}`,
  driver: `${AUTH_IP}:${DRIVER_PORT}`,
  nearest: `${AUTH_IP}:${NEAREST_PORT}`,
  admin: `${AUTH_IP}:${ADMIN_PORT}`,
};
export default Urls;
